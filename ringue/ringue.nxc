task main(){
	SetSensorLight(IN_1);
	while(true){
		if(SENSOR_1 > 70){
			OnFwd(OUT_C, 80);
			OnFwd(OUT_A, 80);
			Wait(300);
		}else{
			Off(OUT_A);
			OnRev(OUT_C, 80);
			Wait(300);
		}
	}
}
